import { RemovehtmltagPipe } from './removehtmltags.pipe';

describe('RemovehtmltagsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovehtmltagPipe();
    expect(pipe).toBeTruthy();
  });
});
